- if opening?
  - links = {Test: 'test_path',
             About: 'about_path',
             Contact:'contact_path',
             MyPage: 'current_shop',
             '営業終了': 'close_path'}
- else
  - links = {Test: 'test_path',
             About: 'about_path',
             Contact:'contact_path',
             '出店する': 'start_path',
             '営業開始': 'open_path'}
%header.mdl-layout__header.mdl-layout__header--waterfall
  .mdl-layout__header-row
    %span.mdl-layout-title
      = link_to 'SHOPEN', root_path
    .mdl-layout-spacer
    .mdl-textfield.mdl-js-textfield.mdl-textfield--expandable.mdl-textfield--floating-label.mdl-textfield--align-right
      %label.mdl-button.mdl-js-button.mdl-button--icon{for: 'waterfall-exp'}
        %i.material-icons search
      .mdl-textfield__expandable-holder
        = form_tag shops_path, method: 'get' do
          = text_field_tag(:search, params[:search], class:'mdl-textfield__input', id:'waterfall-exp')
          = submit_tag '検索'
  .mdl-layout__header-row
    .mdl-layout-spacer
    %nav.mdl-navigation
      - links.each do |key, value|
        = link_to key, send(value), class: 'mdl-navigation__link'
.mdl-layout__drawer
  %span.mdl-layout-title SHOPEN
  %nav.mdl-navigation
    - links.each do |key, value|
      = link_to key, send(value), class: 'mdl-navigation__link'
      /%a.mdl-navigation__link{href: send(value)}= key
